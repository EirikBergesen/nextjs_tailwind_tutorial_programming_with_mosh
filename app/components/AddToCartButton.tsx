'use client'
import React from 'react'

const AddToCartButton = () => {
  return (
    <button className='btn btn-primary' onClick={() => console.log('The product card button was now clicked!')}>Button: Add to cart.</button>
  )
}

export default AddToCartButton